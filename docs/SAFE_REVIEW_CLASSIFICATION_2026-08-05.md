# Phân loại Safe Review — 05/08/2026

Tài liệu này đối chiếu toàn bộ finding trong `AI Prompt/AI Gop y 2.md` với commit gốc `2002e3009788d482b1a6ee49eb8a0ed74d971883`, database local được dựng mới từ migration và code trên branch `review/hardening-20260805`. Đây là gate bắt buộc trước khi sửa mã nguồn.

## 1. Baseline và khả năng khôi phục

- `main`: `2002e3009788d482b1a6ee49eb8a0ed74d971883`; working tree sạch trước khi tạo branch.
- Branch: `review/hardening-20260805`.
- Node `v24.11.0`, npm `11.6.1`, Supabase CLI `2.110.0`.
- Next.js `16.2.12`, React `19.2.6`, `@supabase/supabase-js` `2.111.0`, `@supabase/ssr` `0.12.4`.
- Biến bắt buộc (chỉ ghi tên): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `NEXT_PUBLIC_APP_URL`, `EMAIL_APPS_SCRIPT_URL`, `EMAIL_APPS_SCRIPT_SECRET`, `EMAIL_TEST_RECIPIENT`, `BOOTSTRAP_ADMIN_EMAIL`, `BOOTSTRAP_ADMIN_PASSWORD`, `BOOTSTRAP_ADMIN_NAME`, `PRODUCTION_BASE_URL`, `PRODUCTION_ADMIN_IDENTIFIER`, `PRODUCTION_ADMIN_PASSWORD`.
- Backup trước reset: `backups/safe-review-pre-reset-20260805-data.sql` và `backups/safe-review-pre-reset-20260805-schema.sql` (được gitignore).
- `supabase db reset --local` và seed local: thành công.

## 2. Baseline kiểm thử trước thay đổi

| Lệnh                                                      | Kết quả | Ghi chú                                                                                    |
| --------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------ |
| `npm ci`                                                  | PASS    | Có cảnh báo audit ở dependency dev; audit production riêng không có lỗi.                   |
| `npm run format:check`                                    | FAIL    | 6 file dự án và file sinh trong `supabase/.temp`; là lỗi có sẵn.                           |
| `npm run lint`                                            | PASS    |                                                                                            |
| `npm run typecheck`                                       | PASS    |                                                                                            |
| `npm test`                                                | FAIL    | 27/31 pass; 4 lỗi fixture/cô lập dữ liệu.                                                  |
| `npm run test:e2e`                                        | FAIL    | 18 pass, 19 fail, 2 skip, 7 chưa chạy; nhiều test phụ thuộc thứ tự/fixture sau reset sạch. |
| `npm run build`                                           | PASS    | 34 route build thành công.                                                                 |
| `npm audit --omit=dev`                                    | PASS    | 0 lỗ hổng production.                                                                      |
| `supabase db lint --local --level warning --fail-on none` | PASS    | Không có lỗi schema được báo.                                                              |

Các lỗi test baseline được giữ làm mốc; không được quy cho remediation mới nếu chưa có bằng chứng hồi quy.

## 3. Bảng phân loại bắt buộc

| ID        | Nhóm                   | Severity | Trạng thái      | Bằng chứng                                                                                                                                                   | Cách tái hiện/test                                                       | Tác động                                                                     | Quyết định                                                                                     |
| --------- | ---------------------- | -------: | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| SEC-01    | Schema                 |     High | CONFIRMED       | `01_app.sql` tạo `profiles_select_active_users`, nhưng migration và DB local dùng `profiles_select_self_or_admin`.                                           | Reset DB rồi so `pg_policies` với declarative schema.                    | Drift làm reset/diff khó dự đoán.                                            | Đồng bộ schema bằng migration mới và thêm drift check.                                         |
| RLS-01    | RLS                    |     High | PARTIALLY_FIXED | DB đã giới hạn `profiles`; danh sách nghiệp vụ đi qua RPC scoped. Chưa có inventory/test âm đủ cho toàn bộ bảng nhân sự.                                     | Query `pg_policies`, grants; test lecturer/viewer đọc hồ sơ ngoài scope. | Có thể lộ dữ liệu nhân sự nếu policy/RPC lệch.                               | Giữ mô hình scoped, bổ sung inventory và negative tests.                                       |
| AUTH-01   | Auth                   | Critical | CONFIRMED       | `reschedule_class`: lớp chưa có cả hai giảng viên chỉ kiểm `private.is_active_user()`.                                                                       | Gọi RPC bằng viewer cùng loại phòng với lớp chưa phân công.              | Người không liên quan đổi ngày lớp.                                          | Bắt cùng ma trận manager/owner, thêm test âm.                                                  |
| AUTH-02   | Auth                   |     High | PARTIALLY_FIXED | RLS delete giới hạn manager theo phòng hoặc lecturer tạo lớp Skills; action dựa vào delete/RLS. Scope manager vẫn rộng hơn “của mình” và tài liệu chưa chốt. | Test staff/importer khác người tạo nhưng cùng loại phòng.                | Có thể xóa lịch ngoài ownership mong đợi.                                    | Chuẩn hóa ma trận và test; không thu hẹp admin/staff ngoài yêu cầu nghiệp vụ.                  |
| AUTH-03   | RPC                    |     High | PARTIALLY_FIXED | SECURITY DEFINER hiện có `search_path=''`; vẫn còn overload `create_import_schedule_row` cũ và nhiều EXECUTE cho `authenticated`.                            | Inventory `pg_proc`, `proacl`, function identity arguments.              | Overload/grant cũ làm tăng bề mặt tấn công.                                  | Xóa overload chết, revoke mặc định, cấp quyền tường minh.                                      |
| AUTH-04   | Grants                 |   Medium | PARTIALLY_FIXED | Nhiều bảng cấp DML cho `authenticated` và phụ thuộc RLS; đây là mẫu Supabase hợp lệ nhưng chưa có kiểm thử ma trận đầy đủ.                                   | Query role grants + test từng role/RLS.                                  | Policy sai một điểm có thể mở mutation.                                      | Không revoke mù; siết các bảng chỉ dùng RPC và thêm test.                                      |
| AUTH-05   | Class lifecycle        |     High | ALREADY_FIXED   | `claim_class` chặn cancelled/quá giờ và khóa row; `withdraw_class` yêu cầu assignee, chưa cancelled và chưa bắt đầu.                                         | RPC tests nhận/rút lớp cancelled/quá giờ.                                | Luồng hiện đã đóng trạng thái nguy hiểm.                                     | Chỉ bổ sung regression tests.                                                                  |
| AUTH-06   | Concurrency            |     High | ALREADY_FIXED   | Trigger/exclusion kiểm cả `lecturer_id` và `lecturer_2_id`; RPC bắt `exclusion_violation`.                                                                   | Tạo hai lịch trùng cho giảng viên ở vị trí 1/2.                          | Chống double-booking đã có.                                                  | Giữ và tăng coverage vị trí giảng viên 2.                                                      |
| IMPORT-01 | Import                 |     High | CONFIRMED       | `import_hash_exists` chạy trước; `normalized_row_hash` chỉ có index thường, không unique toàn cục.                                                           | Hai phiên import cùng row chạy đồng thời.                                | Có thể tạo lịch trùng.                                                       | Đưa chống trùng vào RPC/constraint nguyên tử.                                                  |
| IMPORT-02 | Import                 |     High | CONFIRMED       | Row chạy concurrency 6; nếu ghi row lỗi thì batch `failed` dù row trước đã tạo lịch.                                                                         | Gây lỗi ghi import row giữa batch.                                       | Batch failed nhưng dữ liệu đã partial commit.                                | Định nghĩa rõ per-row commit, trạng thái `completed_with_errors`; không giả vờ rollback batch. |
| IMPORT-03 | Import                 |   Medium | CONFIRMED       | Preview và execute cùng tự dựng validation/mapping riêng trong action/component.                                                                             | So cùng input giữa preview và execute sau khi thay rule.                 | UI báo hợp lệ nhưng server xử lý khác.                                       | Trích bộ normalize/validate dùng chung và test contract.                                       |
| IMPORT-04 | Input                  |     High | PARTIALLY_FIXED | Đã giới hạn 500 row và tên file 255; chưa có giới hạn đồng bộ cho mọi text/payload/JSON.                                                                     | Gửi field rất dài hoặc JSON lớn trực tiếp vào action.                    | Tốn tài nguyên/ghi dữ liệu quá dài.                                          | Thêm schema validation và giới hạn độ dài phía server/DB.                                      |
| IMPORT-05 | Performance            |   Medium | CONFIRMED       | Mỗi row gọi `import_hash_exists`, sau đó một RPC create/record riêng.                                                                                        | Import 500 row và đếm request/latency.                                   | N+1 RPC, chậm và dễ lỗi giữa batch.                                          | Tạo RPC batch/set-based có giới hạn.                                                           |
| IMPORT-06 | Semantics              |   Medium | CONFIRMED       | Duplicate hiện dựa hash normalized của lịch từng được import; lịch tạo tay/trường business-equivalent không được định nghĩa rõ.                              | Tạo lịch tay giống row rồi import; đổi field note/name.                  | Kết quả “trùng” khó dự đoán.                                                 | Chốt business key và hiện lý do duplicate rõ ràng.                                             |
| CLASS-01  | Class workflow         |   Medium | CONFIRMED       | `createScheduleDraft`/tài liệu nói draft nhưng schema/RPC hiện ghi `published`.                                                                              | Tạo lịch tay/import và đọc `schedule_status`.                            | Tên API và tài liệu gây hiểu sai.                                            | Đổi tên theo publish hoặc khôi phục draft; theo UX hiện tại chọn đổi tên/docs.                 |
| CLASS-02  | Permissions/docs       |     High | CONFIRMED       | `AI_REVIEW_BRIEF` nói staff/importer tạo draft, không publish/sửa người khác; implementation publish ngay và manager có quyền rộng hơn.                      | So docs, action, RLS và E2E.                                             | Reviewer/operator dùng ma trận sai.                                          | Chuẩn hóa tài liệu theo ma trận hiện hành và thêm tests.                                       |
| CLASS-03  | Deletion               |     High | CONFIRMED       | Class hard-delete; FK từ equipment/basic-medical dùng CASCADE, import row SET NULL.                                                                          | Xóa class có phiếu liên quan trên local.                                 | Mất dữ liệu nghiệp vụ/audit chi tiết.                                        | Chuyển thao tác người dùng sang soft cancel; giữ hard-delete chỉ cho cleanup kiểm soát.        |
| CLASS-04  | Permissions            |     High | CONFIRMED       | `reschedule_class` và `update_class_schedule_details` có nhánh quyền khác nhau; một bên cho active user khi unassigned.                                      | Chạy cả hai RPC với cùng actor/schedule.                                 | Cùng thao tác UI nhưng DB quyết định khác.                                   | Dùng helper quyền chung.                                                                       |
| SHIFT-01  | Shifts                 |     High | CONFIRMED       | `materialize_shift_pattern` xóa mọi `staff_shifts` đang overlap theo staff/date, không chỉ generated từ pattern.                                             | Tạo ca manual overlap rồi materialize pattern.                           | Ca thủ công có thể bị xóa.                                                   | Chỉ thay row generated thuộc pattern; báo conflict thay vì xóa manual.                         |
| SHIFT-02  | Cron                   |   Medium | PARTIALLY_FIXED | Cron có tên ổn định, dùng Asia/Ho_Chi_Minh và materialize theo range; chưa có lock/run log rõ ràng.                                                          | Chạy refresh đồng thời, kiểm cron/job history.                           | Khó audit và có thể tranh chấp.                                              | Thêm advisory lock, run audit và test idempotency.                                             |
| SHIFT-03  | Shift auth             |   Medium | ALREADY_FIXED   | RPC chỉ staff/admin và luôn dùng `auth.uid()` làm staff/creator.                                                                                             | Admin gọi RPC và kiểm row tạo cho chính admin.                           | Không thể tùy ý ghi ca cho người khác qua RPC này.                           | Giữ; docs nêu rõ là “own”.                                                                     |
| EMAIL-01  | Email                  |     High | CONFIRMED       | Apps Script chỉ giữ 1.000 `dedupeKey` trong Script Properties.                                                                                               | Gửi >1000 key rồi replay key cũ.                                         | Có thể gửi lại email cũ.                                                     | Dùng log sheet/DB bền vững làm dedupe có TTL/index.                                            |
| EMAIL-02  | Email security         |     High | CONFIRMED       | Secret bearer nằm trong JSON body; không timestamp/nonce/HMAC.                                                                                               | Replay payload đã bắt được.                                              | Replay/giả mạo nếu secret lộ.                                                | Ký HMAC timestamp + nonce và chống replay.                                                     |
| EMAIL-03  | Information disclosure |      Low | CONFIRMED       | `doGet()` công khai trả `remainingDailyQuota`.                                                                                                               | GET web app không auth.                                                  | Lộ trạng thái quota dịch vụ.                                                 | Chỉ health tối giản hoặc yêu cầu secret.                                                       |
| EMAIL-04  | Reliability            |     High | CONFIRMED       | Update `sent/failed` trong `deliverNotification` không kiểm tra `error`.                                                                                     | Làm DB update thất bại sau khi Apps Script đã gửi.                       | Email đã gửi nhưng DB còn processing, có thể gửi lại.                        | Kiểm tra ACK, lưu provider result và reconciliation.                                           |
| EMAIL-05  | Configuration          |   Medium | CONFIRMED       | Fallback test recipient hard-code `bao.nguyen@eiu.edu.vn`.                                                                                                   | Bỏ env rồi gọi `getEmailTestRecipient`.                                  | Cấu hình thiếu vẫn gửi tới địa chỉ cụ thể.                                   | Bắt buộc env ở production; local fallback chỉ khi NODE_ENV development.                        |
| EMAIL-06  | Race                   |     High | CONFIRMED       | Delivery mode đọc trước claim/delivery; đổi sang off không khóa các job đang processing.                                                                     | Gửi batch chậm rồi chuyển off.                                           | Một số email vẫn gửi sau khi tắt.                                            | Re-check mode ngay trước provider call và generation/version token.                            |
| EMAIL-07  | Retry/quota            |   Medium | PARTIALLY_FIXED | Có queue, attempts, stale retry, `SKIP LOCKED`, nút gửi lại; chưa có quota-aware backoff/dead-letter rõ ràng.                                                | Giả lập quota exhausted và lỗi lặp.                                      | Retry dồn, khó vận hành.                                                     | Thêm backoff, max attempts, dead-letter và quota logging.                                      |
| EQUIP-01  | Equipment              |     High | CONFIRMED       | Action insert request rồi insert items, khi lỗi mới delete bù.                                                                                               | Gây lỗi item insert/cleanup delete.                                      | Có thể còn phiếu rỗng hoặc email/audit sai.                                  | RPC transaction tạo request + items.                                                           |
| EQUIP-02  | Equipment auth         |     High | CONFIRMED       | `manager_confirm_equipment_status` đặc cách hai email cụ thể.                                                                                                | Admin khác email thử xác nhận giao từ trạng thái Mới.                    | Quyền nghiệp vụ gắn định danh cá nhân.                                       | Dùng capability/config/role, bỏ email khỏi DB.                                                 |
| EQUIP-03  | Equipment state        |     High | PARTIALLY_FIXED | RPC khóa row, kiểm role/phase, cho rollback manager và đủ hai xác nhận; test âm/backward/concurrency chưa đầy đủ.                                            | Hai bên ký đồng thời, rollback qua từng state, gọi sai phase.            | Sai state hoặc mất xác nhận nếu có nhánh chưa phủ.                           | Giữ state machine DB, thêm bảng chuyển trạng thái và concurrency tests.                        |
| EQUIP-04  | Privacy/storage        |     High | CONFIRMED       | PNG base64 100–400.000 ký tự lưu trực tiếp trên `equipment_requests`.                                                                                        | Ký nhiều phiếu, đo row/WAL/backup; thử đọc qua select.                   | Phình DB và dữ liệu sinh trắc nhạy cảm nằm trong row.                        | Chuyển Storage private + path/hash; retention và quyền đọc.                                    |
| EQUIP-05  | Deletion               |     High | CONFIRMED       | Admin/staff hard-delete request; items cascade. Có email/audit nhưng bản ghi nghiệp vụ bị mất.                                                               | Xóa request hoàn thành và kiểm tables/log.                               | Mất hồ sơ giao nhận/chữ ký.                                                  | Soft-delete/cancel + lý do; hard purge theo retention.                                         |
| EQUIP-06  | Equipment import       |     High | ALREADY_FIXED   | `import_equipment_requests` có exception block cho từng request; request và toàn bộ items cùng subtransaction, lỗi trả outcome và không để phiếu dở.         | Làm item thứ hai lỗi và kiểm request/items không tồn tại.                | Atomicity từng phiếu đã đáp ứng.                                             | Bổ sung regression test, không đổi kiến trúc.                                                  |
| EQUIP-07  | Export/IDOR            |     High | ALREADY_FIXED   | Route PDF yêu cầu session, role admin/staff, query bằng RLS và trả `private, no-store`.                                                                      | Lecturer gọi URL request bất kỳ.                                         | IDOR đã bị chặn.                                                             | Thêm negative route test.                                                                      |
| UI-01     | Maintainability        |   Medium | CONFIRMED       | Nhiều component 600–1.557 dòng; lớn nhất `equipment-request-list.tsx`.                                                                                       | Báo line count và dependency graph.                                      | Khó review/test, dễ gây hồi quy.                                             | Tách theo feature khi chạm vào; không refactor đại trà chỉ vì line count.                      |
| UI-02     | UI permissions         |     High | CONFIRMED       | Quyền hiển thị rải ở nhiều component/action/RLS; đã có ít nhất ma trận reschedule lệch DB.                                                                   | Snapshot menu/buttons theo role rồi gọi action/RPC trực tiếp.            | UI có thể ẩn/hiện sai; DB vẫn là gate cuối.                                  | Tạo capability matrix dùng chung + test role/page/action.                                      |
| UI-03     | Accessibility          |   Medium | PARTIALLY_FIXED | Có nhiều aria label/dialog/listbox; không có axe/automated a11y suite.                                                                                       | Keyboard-only + axe trên route chính.                                    | Lỗi focus/name/contrast có thể lọt.                                          | Bổ sung axe smoke tests và focus trap regression.                                              |
| UI-04     | Responsive             |   Medium | PARTIALLY_FIXED | Nhiều bảng có overflow/aria hướng dẫn và đã có mobile E2E; coverage không bao trùm popup/lịch/bảng lớn.                                                      | Visual test 360/768/1440 cho route trọng yếu.                            | Nội dung/dropdown có thể tràn hoặc bị che.                                   | Thêm visual/mobile matrix, sửa lỗi quan sát được.                                              |
| UI-05     | Mutation UX            |   Medium | PARTIALLY_FIXED | Nhiều luồng dùng `useTransition`, disable và optimistic state; chưa đồng nhất toàn bộ form/action.                                                           | Double-click/network throttling trên create/import/status.               | Gửi trùng hoặc UI stale.                                                     | Chuẩn hóa pending/idempotency cho mutation trọng yếu.                                          |
| TEST-01   | CI                     |     High | CONFIRMED       | Không có `.github/workflows`.                                                                                                                                | Liệt kê repo/PR checks.                                                  | Không có gate tự động trước merge.                                           | Thêm CI format/lint/typecheck/unit/build và DB tests phù hợp.                                  |
| TEST-02   | Security tests         |     High | CONFIRMED       | Một số E2E quyền có sẵn nhưng thiếu ma trận âm có hệ thống cho RPC/RLS/export.                                                                               | Đối chiếu roles × resource × action.                                     | Lỗi auth dễ tái xuất hiện.                                                   | Thêm integration SQL/API negative tests.                                                       |
| TEST-03   | Concurrency tests      |     High | CONFIRMED       | Có test claim concurrency, thiếu import duplicate/email mode/equipment dual-sign/cron concurrent.                                                            | Chạy promise/transaction song song.                                      | Race condition không bị CI phát hiện.                                        | Thêm suite concurrency độc lập, deterministic.                                                 |
| TEST-04   | Schema drift           |     High | CONFIRMED       | Drift SEC-01 tồn tại; chưa có job reset/diff schema.                                                                                                         | Reset từ declarative/migrations rồi so policies/functions/grants.        | Local/production không cùng contract.                                        | Thêm schema drift script vào CI.                                                               |
| DOC-01    | Documentation          |   Medium | CONFIRMED       | `AI_REVIEW_BRIEF` còn mô tả draft/importer cũ và chứa mật khẩu local mẫu.                                                                                    | So docs với route, DB status và role matrix.                             | Reviewer/operator hiểu sai; thông tin test không nên nằm trong brief review. | Cập nhật docs, chuyển credential local sang seed/dev guide rõ phạm vi.                         |

## 4. Thứ tự remediation đã chốt từ bằng chứng

1. `AUTH-01`, `CLASS-04`, `SEC-01`, inventory `AUTH-03/RLS-01`.
2. `IMPORT-01/02/03/04/05/06`, `SHIFT-01/02`, lifecycle hard-delete.
3. `EMAIL-01..07` theo hướng tương thích Apps Script hiện tại.
4. `EQUIP-01/02/03/04/05`; giữ nguyên các bảo vệ đã đúng ở `EQUIP-06/07`.
5. UI/capability/a11y theo lỗi tái hiện được; tránh refactor lớn không có test.
6. CI, schema drift, security/concurrency tests và tài liệu.

Không triển khai production trong branch này trước khi người dùng duyệt kết quả local.
